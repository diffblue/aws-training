#include <assert.h>

int main(void) {
  int count;

  for (int i = 0; i < count; i++) {
    i = i + 1;
  }
  
  assert(count != 480);
  assert(count != 481);
  assert(count != 482);
  assert(count != 483);
  assert(count != 484);
  assert(count != 485);
  assert(count != 486);
  assert(count != 487);
  assert(count != 488);
  assert(count != 489);
  assert(count != 490);
  assert(count != 491);
  assert(count != 492);
  assert(count != 493);
  assert(count != 494);
  assert(count != 495);
  assert(count != 496);
  assert(count != 497);
  assert(count != 498);
  assert(count != 499);
  
}
